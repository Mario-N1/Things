#include <stdio.h>
#include "common/cursor.h"

int main(int args_count, char** args) {
    StringCursor cursor = cursor_of("Wello");

    /*
     * 1. Parse tokens from text.
     * 1.1. Create cursor on source.
     * 1.2. Read next token while they're available.
     * 1.3. Create a linked list of tokens.
     * 2. Show them.
     */

    return 0;
}
